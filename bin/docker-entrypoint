#!/bin/bash -e

# Always ensure schema exists. Render often starts with extra args
# (e.g. ./bin/rails server -b 0.0.0.0 -p $PORT), so do not require a
# bare "./bin/rails server" match.
./bin/rails db:prepare

exec "${@}"
