#!/bin/bash -e

# Render may override CMD; keep migrations in the start path as well.
mkdir -p /tmp storage tmp/pids log 2>/dev/null || true

echo "[entrypoint] Preparing database..."
./bin/rails db:prepare
echo "[entrypoint] Database ready. Starting: $*"

exec "${@}"
