<% content_for :title, "AliExpress OAuth Callback" %>

<section class="space-y-8">
  <div>
    <p class="text-xs font-medium uppercase tracking-widest text-orange-600">Dropshipping · IOP</p>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">OAuth Callback 就绪</h1>
    <p class="mt-3 max-w-2xl text-slate-600">
      把下面这个 URL <strong>原样</strong>填进速卖通 App Console 的 Callback URL（多一个斜杠、少一段路径都不行）。
      用户授权后会带着 <code class="rounded bg-slate-100 px-1.5 py-0.5 text-sm">code</code>
      回到这里，后端自动换取 <code class="rounded bg-slate-100 px-1.5 py-0.5 text-sm">access_token</code>。
    </p>
  </div>

  <div class="rounded-md border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-950">
    <p class="font-medium">若出现 Sorry, wrong! / temporary failure / Redirect uri does not match</p>
    <p class="mt-1">说明 Console 里登记的 Callback 与下面不一致。改完 Console 后等一两分钟再点授权，
    <strong>不需要</strong>在本地再开开发环境（用 Render 线上即可）。</p>
  </div>

  <div class="rounded-lg border border-slate-200 bg-white p-5">
    <p class="text-xs font-medium uppercase tracking-wide text-slate-500">App Console 必须填的 Callback URL</p>
    <p class="mt-2 break-all font-mono text-sm text-slate-900 select-all"><%= @callback_url %></p>
    <p class="mt-3 text-xs text-slate-500">Render 环境变量 <code>ALIEXPRESS_CALLBACK_URL</code> 也必须是同一串。</p>
  </div>

  <div class="grid gap-4 sm:grid-cols-2">
    <div class="rounded-lg border border-slate-200 bg-white p-5">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">App 凭证</p>
      <% if @configured %>
        <p class="mt-2 text-emerald-700">已配置 App Key / Secret</p>
        <p class="mt-1 font-mono text-xs text-slate-500">client_id=<%= @app_key %></p>
      <% else %>
        <p class="mt-2 text-amber-800">未配置 — 请在 Render Environment 填入 App Key / Secret</p>
      <% end %>
    </div>

    <div class="rounded-lg border border-slate-200 bg-white p-5">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">当前 Token</p>
      <% if @token&.usable? %>
        <p class="mt-2 text-emerald-700">有效 · 过期于 <%= l(@token.expires_at, format: :long) if @token.expires_at %></p>
        <p class="mt-1 truncate font-mono text-xs text-slate-500"><%= @token.access_token.to_s.first(24) %>...</p>
      <% elsif @token %>
        <p class="mt-2 text-amber-800">已过期，请重新授权</p>
      <% else %>
        <p class="mt-2 text-slate-500">尚未授权</p>
      <% end %>
    </div>
  </div>

  <div class="flex flex-wrap items-center gap-3">
    <%= link_to "开始授权 /oauth/authorize", oauth_authorize_path,
        class: "inline-flex items-center rounded-md bg-orange-600 px-4 py-2.5 text-sm font-medium text-white hover:bg-orange-500" %>
  </div>

  <ol class="list-decimal space-y-2 pl-5 text-sm text-slate-600">
    <li>打开速卖通 App Console → 编辑应用 → Callback URL 设为上方地址并保存</li>
    <li>确认 Render 的 <code>ALIEXPRESS_CALLBACK_URL</code> 完全相同</li>
    <li>点击「开始授权」，用<strong>已加入 DS 中心</strong>的卖家账号登录并同意</li>
    <li>成功后会跳到本站 <code>/oauth/success</code>，token 写入 Redis</li>
  </ol>
</section>
