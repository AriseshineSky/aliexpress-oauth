<% content_for :title, "AliExpress OAuth Callback" %>

<section class="space-y-8">
  <div>
    <p class="text-xs font-medium uppercase tracking-widest text-orange-600">Dropshipping · IOP</p>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight text-slate-900">OAuth Callback 就绪</h1>
    <p class="mt-3 max-w-2xl text-slate-600">
      把下面这个 URL 填进速卖通 App Console 的 Callback URL。用户授权后会带着
      <code class="rounded bg-slate-100 px-1.5 py-0.5 text-sm">code</code>
      回到这里，后端自动换取 <code class="rounded bg-slate-100 px-1.5 py-0.5 text-sm">access_token</code>。
    </p>
  </div>

  <div class="rounded-lg border border-slate-200 bg-white p-5">
    <p class="text-xs font-medium uppercase tracking-wide text-slate-500">Callback URL</p>
    <p class="mt-2 break-all font-mono text-sm text-slate-900"><%= @callback_url %></p>
  </div>

  <div class="grid gap-4 sm:grid-cols-2">
    <div class="rounded-lg border border-slate-200 bg-white p-5">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">App 凭证</p>
      <% if @configured %>
        <p class="mt-2 text-emerald-700">已配置 App Key / Secret</p>
      <% else %>
        <p class="mt-2 text-amber-800">未配置 — 请复制 <code>.env.example</code> 为 <code>.env</code> 并填入凭证</p>
      <% end %>
    </div>

    <div class="rounded-lg border border-slate-200 bg-white p-5">
      <p class="text-xs font-medium uppercase tracking-wide text-slate-500">当前 Token</p>
      <% if @token&.usable? %>
        <p class="mt-2 text-emerald-700">有效 · 过期于 <%= l(@token.expires_at, format: :long) if @token.expires_at %></p>
        <p class="mt-1 truncate font-mono text-xs text-slate-500"><%= @token.access_token.to_s.first(24) %>...</p>
      <% elsif @token %>
        <p class="mt-2 text-amber-800">已过期，请重新授权</p>
      <% else %>
        <p class="mt-2 text-slate-500">尚未授权</p>
      <% end %>
    </div>
  </div>

  <div class="flex flex-wrap items-center gap-3">
    <%= link_to "开始授权 /oauth/authorize", oauth_authorize_path,
        class: "inline-flex items-center rounded-md bg-orange-600 px-4 py-2.5 text-sm font-medium text-white hover:bg-orange-500" %>

    <% if @token&.usable? %>
      <%= form_with url: product_path("1005006123456789"), method: :get, class: "flex gap-2" do %>
        <input type="hidden" name="local_country" value="US">
        <button type="submit" class="rounded-md border border-slate-300 bg-white px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50">
          测试产品接口
        </button>
      <% end %>
    <% end %>
  </div>

  <ol class="list-decimal space-y-2 pl-5 text-sm text-slate-600">
    <li>App Console Callback URL 填 <code><%= @callback_url %></code>（须与环境变量一致）</li>
    <li>配置 <code>ALIEXPRESS_APP_KEY</code> / <code>ALIEXPRESS_APP_SECRET</code>（及生产环境 <code>REDIS_URL</code>）</li>
    <li>本地需要 HTTPS 时运行 <code>bin/dev-https</code>；生产用 Render：<code>https://aliexpress-oauth.onrender.com/callback</code></li>
    <li>点击「开始授权」，登录速卖通并同意授权</li>
    <li>回调页自动调用 <code>/auth/token/create</code> 并存入 Redis（带 EXPIRE）</li>
  </ol>

  <% if @callback_url.to_s.start_with?("https://") %>
    <p class="rounded-md bg-emerald-50 px-4 py-3 text-sm text-emerald-800">
      当前 Callback 已是 HTTPS。请确认与 App Console 完全一致。
    </p>
  <% else %>
    <p class="rounded-md bg-amber-50 px-4 py-3 text-sm text-amber-900">
      当前是 HTTP localhost。Console 若要求 HTTPS，请另开终端运行
      <code class="rounded bg-amber-100 px-1">bin/tunnel</code>
      或一键 <code class="rounded bg-amber-100 px-1">bin/dev-https</code>。
    </p>
  <% end %>
</section>
